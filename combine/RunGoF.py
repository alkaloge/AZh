#!/usr/bin/env python

import ROOT
import os
import argparse
import AZh.combine.stylesAZh as styles
import AZh.combine.utilsAZh as utils

def MakeCommandGoF(**kwargs,)

if __name__ = "__main__":

    from argparse import ArgumetParser("RunGoF")
    parser.add_argument('-year','--year',dest='year')
    parser.add_argument('-channel','--channel',dest='channel')
    parser.add_argument('-mass','--mass',dest='mass')
    parset.add_argument('-closure_test','--closure_test',dest='closure',)
